#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
int main()
{
    char text[100] = {false};
    int i = 0;
    scanf("%s", text);
    for(i = 0; i < 100; i++)
    {
        if(i == ',')
        text[i] = 1;
        if('A' <= text[i] && text[i] <= 'Z')
        {
            ch = ch - 'A';
        }
        if('a' <= text[i] && text[i] <= 'z')
        {
            ch = ch - 'a';
        }
    }
    
    for(i = 0; i < 100; i++)
    {   
 

    }


}
